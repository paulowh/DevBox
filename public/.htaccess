RewriteEngine On

# Permitir acesso a arquivos e diretórios existentes (assets, uploads, etc)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirecionar tudo para index.php
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
